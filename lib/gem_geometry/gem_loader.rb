module GemGeometry
  class GemLoader
  def initialize
    # Load using Gem::Package in ruby stdlib
    # Metadata can be obtained via .metadata on the returned object.
    # Iterate with each. Entry has a .read
    # Perhaps exclude test files
  end
end

end
